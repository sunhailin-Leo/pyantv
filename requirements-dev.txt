# ---------------------------------------------------------------------------
# ⚠️ DEPRECATED（Sprint 62 起）：pyproject.toml 已成为依赖声明的 Single Source of Truth。
# 本文件仅保留用于向后兼容，推荐的安装方式是：
#
#     # 推荐：uv（CI 必装，可重现构建）
#     pip install uv && uv sync --dev --extra all
#     # 或等价于：
#     make uv-install
#
#     # Fallback：纯 pip
#     pip install -e '.[dev,test,all]'
#     # 或等价于：
#     make install-dev
#
# 未来版本可能会删除本文件。请尽快迁移到上述任一方式。
# ---------------------------------------------------------------------------
black==25.1.0
isort>=5.13.0